# Travel journal application

See [the project README](../README.md) for capabilities, limitations, photo credits, and the access boundary.

Run `pnpm install`, `pnpm dev`, and `pnpm build` here. The logical storage bindings are DB (D1) and BUCKET (R2). Run `pnpm db:generate` after schema changes and apply generated migrations before deployment.
